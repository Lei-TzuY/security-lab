from pathlib import Path


def replace_one(path: str, old: str, new: str, label: str) -> None:
    p = Path(path)
    text = p.read_text()
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected exactly one match, got {count}")
    p.write_text(text.replace(old, new, 1))


# README: seal 46A on main and describe the verified bounded batch candidate.
replace_one(
    "README.md",
    "The current Milestone 46A verified candidate adds an **independently persisted authenticated snapshot-store head**: a host-held HMAC key authenticates a generation plus complete audited inventory identity in a state root configured separately from the object store, so store-only rollback or divergence fails closed while that independent state remains intact.",
    "Milestone 46A added an **independently persisted authenticated snapshot-store head**: a host-held HMAC key authenticates a generation plus complete audited inventory identity in a state root configured separately from the object store, so store-only rollback or divergence fails closed while that independent state remains intact. The current Milestone 47A verified candidate adds **bounded multi-object publication under one authenticated head generation**: every member is fully archive/signature-prevalidated before mutation, cooperating writers hold one exclusive store transaction across the batch, and a successful batch advances the authenticated head at most once.",
    "README milestone summary",
)
replace_one(
    "README.md",
    "Executable regressions prove wrong-key and state-byte tamper authentication failure, root-overlap rejection, generation advance on a new durable object, stable generation on exact deduplication, and store-only rollback detection: after a previously committed object is removed while the independent state remains intact, verification returns `StoreDiverged` and a later candidate object is rejected before publication.\n",
    "Executable regressions prove wrong-key and state-byte tamper authentication failure, root-overlap rejection, generation advance on a new durable object, stable generation on exact deduplication, and store-only rollback detection: after a previously committed object is removed while the independent state remains intact, verification returns `StoreDiverged` and a later candidate object is rejected before publication.\n\nMilestone 47A adds `store_snapshot_archives_ed25519_durable_with_head_state` for batches of 1 through 16 distinct canonical snapshot identities. Every archive/signature pair is fully validated before either durable store or head-state mutation begins; duplicate canonical identities are rejected during that preflight. The launcher then holds the authenticated head-state exclusive lock and one cooperative exclusive snapshot-store transaction across the whole publication loop, requires the pre-batch audited inventory to equal the authenticated head, and—if at least one object is newly inserted—re-audits the final inventory and durably publishes exactly one generation increment. An all-deduplicated batch leaves the authenticated head unchanged.\n\nThe batch API is deliberately **not** a crash-atomic all-or-nothing filesystem transaction. A deterministic regression forces successor-inventory failure only after two valid objects have been durably inserted: the call returns an error, both objects remain present, the authenticated head remains unchanged, and a later normal verification returns `StoreDiverged`. This makes partial durable advancement fail closed and observable rather than claiming rollback of already-published objects.\n",
    "README 47A evidence",
)

# Threat model: promote 46A and add the bounded 47A failure semantics without overstating atomicity.
replace_one(
    "THREAT_MODEL.md",
    "The current Milestone 46A verified candidate adds a separately persisted HMAC-authenticated generation plus complete inventory identity, providing fail-closed detection of store-only rollback/divergence while the independent state root and host-held key remain intact.",
    "Milestone 46A added a separately persisted HMAC-authenticated generation plus complete inventory identity, providing fail-closed detection of store-only rollback/divergence while the independent state root and host-held key remain intact. The current Milestone 47A verified candidate adds bounded multi-object publication under one authenticated head generation while preserving explicit non-atomic failure semantics.",
    "threat purpose summary",
)
replace_one(
    "THREAT_MODEL.md",
    "Milestones through 45A are integrated on `main`, including bounded COW snapshot lifecycle, authenticated/durable content-addressed frozen-object storage, explicit signer trust-policy rotation/revocation, bounded whole-store audit/inventory identity, cooperative shared-read/exclusive-write store serialization, and optimistic inventory-guarded publication. Milestone 41B persists and authenticates the exact signer-policy identity while keeping privileged whole-directory rollback outside that claim. The current 46A candidate separately authenticates and durably persists a store generation plus complete inventory identity outside the store root, so store-only rollback/divergence is detected while that independent state and its host-held HMAC key remain intact. It does not establish coordinated store+state rollback resistance, hardware monotonicity, remote witnessing, or one crash-atomic transaction spanning both durable resources. Milestone 4A cgroup-v2 aggregate process accounting remains blocked by missing unprivileged delegation. Future promotion must add materially stronger hostile-writer/point-in-time semantics, multi-object atomic mutation, hardware/remote monotonic anchoring, or another distinct executable authority/integration frontier without overstating evidence.",
    "Milestones through 46A are integrated on `main`, including bounded COW snapshot lifecycle, authenticated/durable content-addressed frozen-object storage, explicit signer trust-policy rotation/revocation, bounded whole-store audit/inventory identity, cooperative shared-read/exclusive-write store serialization, optimistic inventory-guarded publication, and an independently authenticated persisted store head. Milestone 41B persists and authenticates the exact signer-policy identity while keeping privileged whole-directory rollback outside that claim. Milestone 46A separately authenticates and durably persists a store generation plus complete inventory identity outside the store root, so store-only rollback/divergence is detected while that independent state and its host-held HMAC key remain intact. The current 47A candidate batches up to 16 prevalidated distinct snapshot identities beneath one authenticated head generation and one cooperative exclusive store transaction; it does not turn the store and head into one crash-atomic resource. Milestone 4A cgroup-v2 aggregate process accounting remains blocked by missing unprivileged delegation. Future promotion must add materially stronger hostile-writer/point-in-time semantics, true multi-object atomic mutation, hardware/remote monotonic anchoring, or another distinct executable authority/integration frontier without overstating evidence.",
    "threat phase status",
)
replace_one(
    "THREAT_MODEL.md",
    "The executable rollback oracle deletes a previously anchored store object while leaving the independent head intact. Verification then returns typed `StoreDiverged`, and a subsequent guarded publication refuses to add its candidate object. This is evidence for store-only rollback/divergence detection under the documented trust assumptions, not proof against a privileged actor that can coherently roll back or replace both roots or obtain/control the HMAC key. Store publication and head-state publication remain separate durable resources rather than a two-resource crash-atomic commit; an error after store advancement leaves a detectable fail-closed mismatch rather than claiming rollback.\n",
    "The executable rollback oracle deletes a previously anchored store object while leaving the independent head intact. Verification then returns typed `StoreDiverged`, and a subsequent guarded publication refuses to add its candidate object. This is evidence for store-only rollback/divergence detection under the documented trust assumptions, not proof against a privileged actor that can coherently roll back or replace both roots or obtain/control the HMAC key. Store publication and head-state publication remain separate durable resources rather than a two-resource crash-atomic commit; an error after store advancement leaves a detectable fail-closed mismatch rather than claiming rollback.\n\nMilestone 47A accepts 1–16 batch members and completely validates each archive/signature plus duplicate canonical identity before filesystem mutation. The authenticated head lock is ordered before one exclusive snapshot-store transaction held across every member; the pre-batch inventory must equal the authenticated head, successful inserted batches publish one final successor inventory at generation+1, and an all-deduplicated batch leaves the head unchanged. Cooperative readers therefore serialize around the batch transaction rather than observing an intentionally published intermediate inventory.\n\nThis is still not a crash-atomic multi-object transaction. Deterministic evidence forces the final successor inventory audit to fail after two valid objects have already been durably inserted. The call fails, the old authenticated head remains, both durable objects remain, and a later verification returns `StoreDiverged`. No rollback of earlier durable members, hostile non-cooperating writer serialization, point-in-time live-filesystem snapshot, coordinated state+store rollback resistance, or hardware/remote monotonic witness is claimed.\n",
    "threat 47A semantics",
)

# Roadmap: seal 46A and promote the tested 47A slice.
replace_one(
    "ROADMAP.md",
    "**Current verified candidate.** Adds one independently persisted host-side generation/inventory anchor outside the snapshot object store, rather than another caller-retained comparison token.",
    "**Status: complete on `main`.** Adds one independently persisted host-side generation/inventory anchor outside the snapshot object store, rather than another caller-retained comparison token.",
    "roadmap 46A status",
)
replace_one(
    "ROADMAP.md",
    "### Milestone 46 promotion rule\n\nAfter 46A integrates, seal caller-hosted local HMAC head-state rollback detection. Do not farm generation formatting, extra MAC encodings, or retry wrappers. A stronger snapshot-store phase must add materially different hostile-writer/point-in-time semantics, genuine multi-object atomic mutation, or an external/hardware monotonic witness with executable evidence; otherwise promote to another independent authority frontier.\n\n## Independent host-local IPC frontier — post-launch object transfer\n",
    "### Milestone 46 promotion rule\n\nAfter 46A integrates, seal caller-hosted local HMAC head-state rollback detection. Do not farm generation formatting, extra MAC encodings, or retry wrappers. A stronger snapshot-store phase must add materially different hostile-writer/point-in-time semantics, genuine multi-object atomic mutation, or an external/hardware monotonic witness with executable evidence; otherwise promote to another independent authority frontier.\n\n## Milestone 47 — bounded authenticated multi-object publication\n\n### Slice 47A — one-generation batch head publication\n\n**Current verified candidate.** Extends the authenticated head from one-object guarded publication to a bounded cooperative batch without claiming crash-atomic rollback of durable members.\n\nAcceptance evidence is executable:\n\n- the public batch request accepts 1 through `SNAPSHOT_STORE_HEAD_MAX_BATCH_ITEMS` (16) members; each member carries one archive, Ed25519 public key/signature, and archive limits;\n- every archive/signature pair is completely validated before either durable store or head-state mutation, and duplicate canonical identities are rejected during the same preflight;\n- one authenticated head-state exclusive lock is acquired before one exclusive snapshot-store transaction that remains held across the whole batch; the current audited store inventory must exactly equal the authenticated head before the first member is published;\n- a two-object valid batch durably inserts both objects, re-audits the complete successor inventory, and advances the authenticated head exactly once from generation 1 to generation 2; replaying the same batch deduplicates both objects and leaves the generation unchanged;\n- an invalid second signature is rejected before the first object is published, and a duplicate canonical identity is rejected before store mutation;\n- a deterministic post-store failure oracle uses a successor inventory budget that passes the initial empty-store check but fails only after two valid objects have been durably inserted. The call returns an error, both objects remain in the store, the authenticated head stays at its prior generation, and a later normal-budget verification returns typed `StoreDiverged` with two actual objects;\n- exact formal rustfmt, Clippy with `-D warnings`, complete stable tests, and the complete Rust 1.74 suite are green.\n\nBoundary: 47A is a bounded cooperative publication unit, not a crash-atomic all-or-nothing filesystem transaction. A failure after one or more durable object insertions may leave those objects present while the authenticated head remains old; the supported guarantee is fail-closed divergence detection on later verification, not rollback. It does not serialize hostile non-cooperating writers, freeze a hostile live filesystem, provide coordinated store+state rollback resistance, or add TPM/secure-counter/remote-witness monotonicity.\n\n### Milestone 47 promotion rule\n\nAfter 47A integrates, seal bounded cooperative batch publication under one authenticated head generation. Do not farm larger caps, batch aliases, retry wrappers, or alternate duplicate spellings. A stronger snapshot-store phase must add true crash-atomic/point-in-time semantics or an external/hardware monotonic witness with executable evidence; otherwise promote to another independent authority frontier.\n\n## Independent host-local IPC frontier — post-launch object transfer\n",
    "roadmap 47A section",
)
