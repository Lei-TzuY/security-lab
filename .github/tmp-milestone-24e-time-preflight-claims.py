from pathlib import Path


def replace_one(path: str, old: str, new: str, label: str) -> None:
    p = Path(path)
    text = p.read_text()
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected exactly one match, got {count}")
    p.write_text(text.replace(old, new, 1))


# README: seal 24D and describe only the verified 24E time-namespace probe.
replace_one(
    "README.md",
    "The current Milestone 24D verified candidate adds a separate **read-only configured-filesystem anchor probe** for the selected root, executable, cwd, optional scratch/proc targets, and declared persistent-volume sources/targets. Positive anchor evidence still does not upgrade the complete mandatory launch core from `unprobed`.",
    "Milestone 24D added a separate **read-only configured-filesystem anchor probe** for the selected root, executable, cwd, optional scratch/proc targets, and declared persistent-volume sources/targets. The current Milestone 24E verified candidate adds a **policy-specific isolated time-namespace probe**: when offsets are requested, a throwaway helper mirrors production user/time-namespace setup, installs the exact declared MONOTONIC/BOOTTIME offsets, and verifies a descendant observes those offsets without touching the configured root or executing the target. Positive 24D/24E evidence still does not upgrade the complete mandatory launch core from `unprobed`.",
    "README milestone summary",
)
replace_one(
    "README.md",
    "Milestone 24D separately performs read-only `openat2`/`fstat` checks against the configured root, executable, cwd, optional scratch/proc targets, and declared persistent-volume source/target anchors without creating namespaces, mounting, writing configured state, or executing the target. A known configured-filesystem failure is `incompatible` / exit status 3; positive configured-anchor evidence still leaves `mandatory_launch_core` intentionally `unprobed`, so the production path remains `indeterminate` / exit status 4 rather than treating point-in-time path compatibility as complete launch compatibility. `launch_attempted=false` and `launch_preflight_complete=false` remain explicit.",
    "Milestone 24D separately performs read-only `openat2`/`fstat` checks against the configured root, executable, cwd, optional scratch/proc targets, and declared persistent-volume source/target anchors without creating namespaces, mounting, writing configured state, or executing the target. Milestone 24E separately probes requested time offsets in a throwaway helper: it creates only user/time namespaces, mirrors production `setgroups`/UID/GID mapping, writes the exact configured `monotonic`/`boottime` entries to `/proc/self/timens_offsets`, forks one observer descendant, and requires each observed clock minus its configured offset to fall inside the helper's before/after host-clock bracket. A failed requested time probe is `incompatible`; a successful time probe is positive prerequisite evidence only. The primary `mandatory_launch_core` remains intentionally `unprobed`, so the production path remains `indeterminate` / exit status 4 rather than treating isolated time/filesystem evidence as complete launch compatibility. `launch_attempted=false` and `launch_preflight_complete=false` remain explicit.",
    "README preflight command",
)

# Threat model: distinguish 24D merged path evidence from 24E isolated time evidence.
replace_one(
    "THREAT_MODEL.md",
    "The current Milestone 24D verified candidate separately inspects configured filesystem anchors with read-only `openat2`/`fstat` operations, while keeping point-in-time path evidence distinct from the still-unprobed complete mandatory launch core.",
    "Milestone 24D separately inspects configured filesystem anchors with read-only `openat2`/`fstat` operations. The current Milestone 24E verified candidate independently probes requested descendant time-namespace offsets in a throwaway user/time namespace and keeps that positive evidence distinct from the still-unprobed complete mandatory launch core.",
    "threat purpose",
)
replace_one(
    "THREAT_MODEL.md",
    "The 24D configured-filesystem probe intentionally inspects the selected root, executable, cwd, optional scratch/proc targets, and declared persistent-volume source/target anchors using only read-only `openat2`/`fstat` operations; it creates no namespaces or mounts, writes no configured filesystem state, and executes no target. A known anchor failure is incompatible evidence, but successful anchor resolution is only a point-in-time prerequisite observation: the complete `mandatory_launch_core` remains `unprobed`, so production preflight still cannot claim full satisfaction. `launch_attempted=false` and `launch_preflight_complete=false` remain explicit.",
    "The 24D configured-filesystem probe intentionally inspects the selected root, executable, cwd, optional scratch/proc targets, and declared persistent-volume source/target anchors using only read-only `openat2`/`fstat` operations; it creates no namespaces or mounts, writes no configured filesystem state, and executes no target. The 24E time probe receives only the requested offsets, creates a throwaway user/time namespace, mirrors production user mapping and `/proc/self/timens_offsets` writes, and forks one observer descendant. Its positive oracle subtracts each configured MONOTONIC/BOOTTIME offset from the descendant reading and requires the adjusted value to lie inside the helper's before/after host-clock bracket; it never receives or touches the configured root and never executes the target. A known anchor or requested-time probe failure is incompatible evidence, but successful isolated probes remain prerequisites only: the complete `mandatory_launch_core` remains `unprobed`, so production preflight still cannot claim full satisfaction. `launch_attempted=false` and `launch_preflight_complete=false` remain explicit.",
    "threat preflight property",
)

# Roadmap: close 24D, add 24E, and keep the promotion boundary conservative.
replace_one(
    "ROADMAP.md",
    "**Current verified candidate.** Adds a policy-specific read-only prerequisite probe for configured filesystem anchors without converting preflight into a launch dry-run.",
    "**Status: complete on `main`.** Adds a policy-specific read-only prerequisite probe for configured filesystem anchors without converting preflight into a launch dry-run.",
    "roadmap 24D status",
)
replace_one(
    "ROADMAP.md",
    "Boundary: 24D is point-in-time read-only path/type/mode prerequisite evidence. It does not pin these descriptors for a later launch, prove cross-time inode identity, inspect the final post-mount Landlock tree, establish time/procfs mount success, reproduce PID1/target orchestration, validate the exact production seccomp program, or prove successful `execveat`.\n\n### Milestone 24 promotion rule\n\n24A–24C are sealed on `main`; 24D is the current integration candidate. After 24D integrates, do not farm more path aliases, errno cases, or anchor spellings around the same read-only preflight mechanism. Another preflight slice is justified only if it closes a materially different mandatory prerequisite without turning preflight into a privileged/destructive launch simulation; otherwise promote to a different executable authority/enforcement frontier. Milestone 25A remains a separate evidence class because it records stages positively observed during an actual run.\n",
    "Boundary: 24D is point-in-time read-only path/type/mode prerequisite evidence. It does not pin these descriptors for a later launch, prove cross-time inode identity, inspect the final post-mount Landlock tree, establish time/procfs mount success, reproduce PID1/target orchestration, validate the exact production seccomp program, or prove successful `execveat`.\n\n### Slice 24E — configured time-namespace offset preflight\n\n**Current verified candidate.** Closes the separately requested time-namespace prerequisite without converting preflight into target execution or a privileged launch simulation.\n\nAcceptance evidence is executable:\n\n- when both policy time offsets are requested, `preflight` / `preflight-json` fork a throwaway helper that receives only those numeric offsets; the helper never receives the configured root and never executes the target;\n- the helper mirrors the production permission sequence with `unshare(CLONE_NEWUSER | CLONE_NEWTIME)`, `setgroups=deny`, UID/GID mapping, then writes exact `monotonic <seconds> 0` and `boottime <seconds> 0` entries to `/proc/self/timens_offsets`;\n- because the caller of `unshare(CLONE_NEWTIME)` remains on the original clock view while subsequently created descendants enter the prepared child time namespace, the helper brackets host `CLOCK_MONOTONIC` / `CLOCK_BOOTTIME` immediately before and after one observer descendant reads both clocks; subtracting each declared offset from the observer value must fall inside the matching host-clock bracket;\n- the probe reports its exact first failed stage plus errno. A requested unsupported/denied time prerequisite is `incompatible`; a complete observer oracle reports `supported`;\n- machine evidence records `isolated_helper=true`, `configured_root_touched=false`, `target_executed=false`, and the exact requested offset pair. Existing CLI regression coverage requires even 1/2-second offsets to pass the strict bracket oracle rather than relying on a broad timing tolerance;\n- the primary `mandatory_launch_core` deliberately remains `unprobed`, so real production preflight with otherwise positive 24C/24D/24E evidence remains `indeterminate` / exit status 4 and keeps `launch_attempted=false` / `launch_preflight_complete=false`;\n- stable rustfmt/Clippy/full tests and the full Rust 1.74 suite are green on the exact implementation head.\n\nBoundary: 24E proves that the current host can create an isolated user/time namespace and install/observe the exact requested MONOTONIC/BOOTTIME offsets in a descendant. It does not prove private procfs setup, the complete namespace/mount/PID1/target orchestration, final filesystem/Landlock state, the exact production seccomp program, or successful `execveat`; actual runtime execution and enforcement receipts remain a separate evidence class.\n\n### Milestone 24 promotion rule\n\n24A–24D are sealed on `main`; 24E is the current integration candidate. After 24E integrates, do not farm clock IDs, offset values, path aliases, errno cases, or duplicate isolated probes. Another preflight slice is justified only if it closes a materially different mandatory prerequisite without turning preflight into a privileged/destructive launch simulation; otherwise promote to a different executable authority/enforcement frontier. Milestone 25A remains a separate evidence class because it records stages positively observed during an actual run.\n",
    "roadmap 24E section",
)
