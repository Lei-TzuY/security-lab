from pathlib import Path


def replace_one(path: str, old: str, new: str, label: str) -> None:
    p = Path(path)
    text = p.read_text()
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected exactly one match, got {count}")
    p.write_text(text.replace(old, new, 1))


replace_one(
    "README.md",
    "The current Milestone 39A verified candidate adds `materialize_snapshot_archive_ed25519_atomic(archive, destination, public_key, signature, limits)`. The function first validates the complete canonical archive under the existing archive byte/identity byte/node ceilings, derives the Milestone 33A identity directly from those frozen records, and then reuses the Milestone 37A `ed25519-dalek` strict verifier over the exact supplied public key and 64-byte signature. Only a successfully verified artifact may enter the existing 38A fd-relative staging and `renameat2(RENAME_NOREPLACE)` publication path.\n",
    "Milestone 39A added `materialize_snapshot_archive_ed25519_atomic(archive, destination, public_key, signature, limits)`: the complete bounded canonical archive is parsed first, its Milestone 33A identity is derived from frozen records, and the existing Milestone 37A strict Ed25519 verifier must accept the exact caller-supplied public key/signature before destination inspection, staging, or `renameat2(RENAME_NOREPLACE)` publication. The integrated `snapshot-publish-ed25519` CLI is a bounded tooling surface over that same gate rather than a separate trust claim.\n\n## Authenticated content-addressed snapshot store\n\nThe current Milestone 40A verified candidate adds a trusted host-local content-addressed store for those authenticated frozen archives. `store_snapshot_archive_ed25519_atomic` validates the bounded archive and strictly verifies its Ed25519 evidence before touching the store filesystem, then addresses the object by the complete canonical identity tuple `(sha256, encoded_bytes, nodes)` and publishes a read-only object with `renameat2(RENAME_NOREPLACE)`. A pre-existing object at the same address is a deduplication hit only after regular-file/read-only checks, exact length, and byte-for-byte equality; conflicting bytes are never overwritten. `materialize_snapshot_store_object_ed25519_atomic` reopens the exact identity-addressed object, re-derives and matches its canonical identity, re-verifies the supplied signature, and only then enters the existing failure-atomic archive materialization path.\n\nExecutable evidence proves first insertion plus idempotent deduplication, restoration of the frozen archive after the live source is mutated, wrong-key rejection before a deliberately missing store root is inspected, and fail-closed rejection of a same-size parse-valid stored-object tamper with no destination publication. Store objects are set to mode `0444` by the API, but this is logical host-local immutability, not protection from a privileged writer that controls the store root. The slice does not claim `fsync` crash durability, trust-store/key provenance or rotation/revocation, garbage collection/indexing, version retention, distributed CAS semantics, or hostile-store-writer resistance.\n",
    "README 39A/40A section",
)

replace_one(
    "ROADMAP.md",
    "**Current verified candidate.** Composes the existing 37A strict public-key signature verifier with the 38A frozen archive/materialization path so unauthenticated archive bytes cannot reach destination inspection or staging.\n",
    "**Status: complete on `main`.** Composes the existing 37A strict public-key signature verifier with the 38A frozen archive/materialization path so unauthenticated archive bytes cannot reach destination inspection or staging.\n",
    "roadmap 39A status",
)

replace_one(
    "ROADMAP.md",
    "### Milestone 39 promotion rule\n\nAfter 39A integrates, do not farm signature encodings, duplicate verify wrappers, key-file spelling, or extra tamper vectors that exercise the same gate. Promote only to a real trust/key lifecycle, crash-durable/versioned publication, or another independent executable authority boundary.\n\n## Independent host-local IPC frontier — post-launch object transfer\n",
    "### Milestone 39 promotion rule\n\n39A is sealed on `main`; the bounded verified publication CLI is integrated tooling over the same gate and does not widen the trust model. Do not farm signature encodings, duplicate verify wrappers, key-file spelling, or extra tamper vectors that exercise the same gate.\n\n## Milestone 40 — authenticated content-addressed snapshot storage\n\n### Slice 40A — immutable identity-addressed frozen archive objects\n\n**Current verified candidate.** Adds a bounded host-local object lifecycle for already-authenticated frozen archives rather than another signature or publication wrapper.\n\nAcceptance evidence is executable:\n\n- `store_snapshot_archive_ed25519_atomic(store_root, archive, public_key, signature, limits)` lexically validates a trusted absolute store root, fully validates the canonical archive under the existing byte/identity/node limits, and strictly verifies its Ed25519 evidence before store filesystem inspection or mutation;\n- the deterministic object address encodes the complete canonical identity tuple `(sha256, encoded_bytes, nodes)`, preserving the accounting covered by the signature message rather than treating the digest alone as the whole identity;\n- insertion creates a private sibling temporary object, writes the exact archive bytes, sets mode `0444`, and publishes only with Linux `renameat2(RENAME_NOREPLACE)`; there is no overwrite fallback;\n- an existing object at the same address is accepted as an idempotent deduplication hit only if it is a regular file with no write bits, has the exact archive length, and matches every archive byte. Any mismatch is `ObjectConflict`;\n- `materialize_snapshot_store_object_ed25519_atomic` opens the exact requested address, enforces the archive byte ceiling, re-derives the canonical identity and requires an exact tuple match, re-verifies the supplied Ed25519 signature, and only then delegates to the existing failure-atomic authenticated materialization path;\n- deterministic integration evidence proves first insert plus dedup, materialization of frozen bytes after the live source changes, wrong-key failure before a deliberately missing store root is inspected, and rejection of a same-size parse-valid stored-object tamper with no destination publication;\n- rustfmt, Clippy with `-D warnings`, the full stable suite, and the full Rust 1.74 suite are green on the exact implementation head.\n\nBoundary: 40A is a trusted host-local bounded object store. Mode `0444` is an API immutability convention, not protection from a privileged writer controlling the store root. It does not provide `fsync` crash durability/recovery, trust-store or key provenance/rotation/revocation, garbage collection/indexing, version retention, remote/distributed CAS, replication, or hostile concurrent store-writer protection.\n\n### Milestone 40 promotion rule\n\nAfter 40A integrates, do not farm object filename encodings, extra dedup aliases, or repeated tamper vectors. Promote to a materially stronger lifecycle boundary such as crash-durable publication/recovery or independently specified trust/key lifecycle semantics.\n\n## Independent host-local IPC frontier — post-launch object transfer\n",
    "roadmap 40A section",
)

replace_one(
    "THREAT_MODEL.md",
    "The current Milestone 39A verified candidate requires that artifact's canonical identity to pass the existing strict Ed25519 verification path under the exact caller-supplied public key/signature before destination inspection, staging, or publication.",
    "Milestone 39A requires that artifact's canonical identity to pass the existing strict Ed25519 verification path under the exact caller-supplied public key/signature before destination inspection, staging, or publication. The current Milestone 40A verified candidate adds authenticated host-local content-addressed storage keyed by the complete canonical identity tuple, with no-replace publication, exact-byte deduplication, and identity/signature re-verification before materialization.",
    "threat purpose 40A",
)

replace_one(
    "THREAT_MODEL.md",
    "## Explicit non-goals and limitations\n",
    "## Content-addressed snapshot-store semantics\n\nMilestone 40A treats the store root as trusted host-local state. A put validates the complete bounded archive and verifies the exact supplied Ed25519 evidence before filesystem inspection or mutation. The object key contains the canonical SHA-256 plus encoded-byte and node accounting; publication uses `renameat2(RENAME_NOREPLACE)`, and an existing key is a deduplication hit only after regular-file/read-only, exact-length, and byte-for-byte checks. Retrieval re-derives the identity from stored bytes and re-verifies the signature before entering authenticated materialization. Stored files are chmod `0444`, which prevents accidental API-side mutation but does not defend against a privileged or otherwise authorized hostile writer controlling the store root. No `fsync` crash durability, key provenance/trust lifecycle, garbage collection, distributed-store, replication, or hostile concurrent-writer property is claimed.\n\n## Explicit non-goals and limitations\n",
    "threat store semantics",
)
