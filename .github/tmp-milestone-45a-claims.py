from pathlib import Path


def replace_one(path: str, old: str, new: str, label: str) -> None:
    p = Path(path)
    text = p.read_text()
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected exactly one match, got {count}")
    p.write_text(text.replace(old, new, 1))


replace_one(
    "README.md",
    "The current Milestone 43A verified candidate adds a **deterministic audited snapshot-store inventory identity** for externally retained cross-run membership comparison.",
    "Milestone 43A added a **deterministic audited snapshot-store inventory identity** for externally retained cross-run membership comparison. Milestone 44A added **cooperative shared-read/exclusive-write snapshot-store transactions**. The current Milestone 45A verified candidate adds **inventory-guarded durable writes** that reject a stale caller-retained whole-store identity before object publication.",
    "README milestone summary",
)
replace_one(
    "README.md",
    "The current Milestone 43A verified candidate adds `snapshot_store_inventory_identity(store_root, limits)` and `verify_snapshot_store_inventory_identity(store_root, expected, limits)` as a cross-run identity layer above the complete Milestone 42A audit.",
    "Milestone 43A added `snapshot_store_inventory_identity(store_root, limits)` and `verify_snapshot_store_inventory_identity(store_root, expected, limits)` as a cross-run identity layer above the complete Milestone 42A audit.",
    "README 43A status",
)
replace_one(
    "README.md",
    "The current Milestone 44A verified candidate makes the store's previous “quiescent or cooperatively serialized” precondition executable for participating callers. `SnapshotStoreReadTransaction` holds a shared Linux `flock` on the already-existing store-root directory and offers the complete store audit, inventory identity, and expected-inventory verification while that lock is live. `SnapshotStoreWriteTransaction` holds an exclusive lock on the same directory inode and offers authenticated durable snapshot publication. Blocking `begin` and nonblocking `try_begin` variants are explicit; a nonblocking conflict returns `SnapshotStoreTransactionError::LockContended` with the requested read/write mode.",
    "Milestone 44A makes the store's previous “quiescent or cooperatively serialized” precondition executable for participating callers. `SnapshotStoreReadTransaction` holds a shared Linux `flock` on the already-existing store-root directory and offers the complete store audit, inventory identity, and expected-inventory verification while that lock is live. `SnapshotStoreWriteTransaction` holds an exclusive lock on the same directory inode and offers authenticated durable snapshot publication. Blocking `begin` and nonblocking `try_begin` variants are explicit; a nonblocking conflict returns `SnapshotStoreTransactionError::LockContended` with the requested read/write mode. Milestone 45A adds `SnapshotStoreWriteTransaction::store_ed25519_durable_if_inventory`: while that same exclusive lock is live, it recomputes the bounded audited whole-store identity and returns typed `InventoryConflict { expected, actual }` before the supplied archive reaches publication when the caller's retained identity is stale. A matching identity proceeds through the unchanged authenticated `fsync`-backed durable publication path, and `SnapshotStoreWriteTransaction::inventory_identity` can obtain the successor identity before releasing the writer lock.",
    "README transaction semantics",
)
replace_one(
    "README.md",
    "Executable regressions prove shared-reader coexistence, read/write and write/write exclusion, typed nonblocking contention, RAII release, authenticated durable publication under the exclusive transaction, and complete one-object → two-object inventory transitions under shared read transactions. The exact candidate remains covered by stable rustfmt/Clippy/full tests and the complete Rust 1.74 suite.",
    "Executable regressions prove shared-reader coexistence, read/write and write/write exclusion, typed nonblocking contention, RAII release, authenticated durable publication under the exclusive transaction, and complete one-object → two-object inventory transitions under shared read transactions. The 45A regression then retains the one-object identity, advances the store to two objects, proves a stale guarded attempt returns the exact expected/actual identities without publishing its third object, retries with the current two-object identity, and obtains the three-object successor identity while the same writer still excludes readers. This is optimistic cooperation, not rollback protection: legacy/non-cooperating writers can bypass the advisory lock, and a caller must retain the expected identity in whatever trust domain its application requires. The exact candidate remains covered by stable rustfmt/Clippy/full tests and the complete Rust 1.74 suite.",
    "README 45A evidence",
)

replace_one(
    "ROADMAP.md",
    "**Current verified candidate.** Adds an explicit host-local cooperation protocol around durable publication and whole-store read operations instead of pretending the existing read-only audit is a concurrent filesystem snapshot.",
    "**Status: complete on `main`.** Adds an explicit host-local cooperation protocol around durable publication and whole-store read operations instead of pretending the existing read-only audit is a concurrent filesystem snapshot.",
    "ROADMAP 44A status",
)
replace_one(
    "ROADMAP.md",
    "### Milestone 44 promotion rule\n\nAfter 44A integrates, seal the shared/exclusive advisory-lock vocabulary. Do not farm lock-name aliases, timeout knobs, or more reader-count variants. A stronger store-lifecycle phase must introduce materially new semantics such as independently anchored rollback state, hostile/non-cooperating mutation detection, or a genuine point-in-time snapshot/transaction mechanism with deterministic evidence; otherwise promote to another independent authority frontier.\n",
    "### Milestone 44 promotion rule\n\n44A is sealed on `main`. Do not farm lock-name aliases, timeout knobs, or more reader-count variants. Stronger store-lifecycle work must add materially new mutation/concurrency semantics rather than another lock spelling.\n\n## Milestone 45 — inventory-guarded cooperative mutation\n\n### Slice 45A — optimistic whole-store precondition for durable publication\n\n**Current verified candidate.** Composes the 43A complete-store commitment, the 44A exclusive transaction, and the 40B authenticated durable publication path into one compare-and-publish boundary for cooperating writers.\n\nAcceptance evidence is executable:\n\n- `SnapshotStoreWriteTransaction::inventory_identity` derives the complete bounded audited inventory while the exclusive transaction lock remains live, allowing a writer to retain a successor identity before releasing serialization;\n- `store_ed25519_durable_if_inventory` recomputes that inventory under the same exclusive lock and compares it with a caller-retained expected identity before invoking the object publication path;\n- mismatch returns typed `SnapshotStoreTransactionError::InventoryConflict { expected, actual }`, preserving both complete identities for caller conflict handling;\n- deterministic regression captures a one-object identity, lets an intervening cooperating writer advance the store to two objects, then proves a stale guarded attempt reports expected=one/actual=two and leaves the candidate third-object content address absent;\n- using the current two-object identity permits authenticated `fsync`-backed publication of that third object, after which the same still-live writer derives a distinct three-object successor identity while a cooperating reader remains lock-contended;\n- after writer release, an independent read transaction observes exactly that successor identity; existing audit, inventory, transaction, durable-store, trust, sandbox, and tooling regressions remain active;\n- exact candidate rustfmt, Clippy with `-D warnings`, complete stable tests, and the complete Rust 1.74 suite are green.\n\nBoundary: 45A is optimistic concurrency control only among callers that cooperate with the 44A store-root lock and carry a previously observed 43A inventory identity. It is not an independently authenticated or monotonic rollback anchor, does not stop privileged/non-cooperating filesystem mutation, does not roll back a publication after its durable path begins, and does not provide general database transactions, multi-object atomic commits, or distributed compare-and-swap.\n\n### Milestone 45 promotion rule\n\nAfter 45A integrates, seal simple expected-inventory guarded single-object publication. Do not farm extra conflict spellings, retry helpers, or token wrappers. A stronger snapshot-store phase must add independently anchored history/rollback semantics, hostile-writer detection, multi-object atomic mutation, or a genuine point-in-time filesystem/store mechanism with executable evidence; otherwise promote to another authority frontier.\n",
    "ROADMAP 45A promotion",
)

replace_one(
    "THREAT_MODEL.md",
    "Milestones through 41B are integrated on `main`, including bounded COW snapshot lifecycle, authenticated/durable content-addressed frozen-object storage, and explicit signer trust-policy rotation/revocation. Milestone 41B persists and authenticates the exact policy identity, serializes cooperating rotations, and rejects stale caller policy before state-backed store/materialization access while keeping privileged whole-directory rollback outside the claim. Milestone 4A cgroup-v2 aggregate process accounting remains blocked by missing unprivileged delegation; supplementary-group isolation also remains blocked on a viable mapping architecture. Future promotion must target a materially stronger independently anchored trust state or another distinct executable authority/integration frontier without overstating evidence.",
    "Milestones through 44A are integrated on `main`, including bounded COW snapshot lifecycle, authenticated/durable content-addressed frozen-object storage, explicit signer trust-policy rotation/revocation, bounded whole-store audit/inventory identity, and cooperative shared-read/exclusive-write store serialization. Milestone 41B persists and authenticates the exact policy identity while keeping privileged whole-directory rollback outside the claim. The current 45A candidate adds an optimistic expected-inventory precondition to cooperating durable writers; it does not turn that unkeyed inventory value into an independently anchored rollback state. Milestone 4A cgroup-v2 aggregate process accounting remains blocked by missing unprivileged delegation. Future promotion must target materially stronger independently anchored history/rollback semantics, hostile-writer detection, genuine multi-object/point-in-time transactions, or another distinct executable authority/integration frontier without overstating evidence.",
    "THREAT_MODEL promotion status",
)
replace_one(
    "THREAT_MODEL.md",
    "Milestone 44A adds a caller-visible cooperation mechanism for store-wide reads versus durable publication. A read transaction opens the pre-existing store-root directory with `O_NOFOLLOW` and holds `flock(LOCK_SH)` for its lifetime; a write transaction opens the same directory and holds `flock(LOCK_EX)`. Whole-store audit/inventory operations are methods of the shared transaction, and authenticated durable publication is a method of the exclusive transaction, so cooperating callers using the same local store-root inode cannot overlap those operations. `try_begin` uses `LOCK_NB` and reports typed contention instead of performing an unlocked fallback.",
    "Milestone 44A adds a caller-visible cooperation mechanism for store-wide reads versus durable publication. A read transaction opens the pre-existing store-root directory with `O_NOFOLLOW` and holds `flock(LOCK_SH)` for its lifetime; a write transaction opens the same directory and holds `flock(LOCK_EX)`. Whole-store audit/inventory operations are methods of the shared transaction, and authenticated durable publication is a method of the exclusive transaction, so cooperating callers using the same local store-root inode cannot overlap those operations. `try_begin` uses `LOCK_NB` and reports typed contention instead of performing an unlocked fallback. Milestone 45A adds an optimistic mutation precondition inside that already-exclusive writer: the launcher recomputes the bounded 43A inventory, returns typed `InventoryConflict { expected, actual }` before publication on mismatch, or reuses the existing authenticated durable single-object publication on a match. The writer can then derive the successor inventory while it still owns the exclusive lock.",
    "THREAT_MODEL 45A mechanism",
)
replace_one(
    "THREAT_MODEL.md",
    "This is an advisory serialization claim only. Existing direct snapshot-store APIs remain outside the lock protocol for compatibility, and a caller that bypasses the transaction types is non-cooperating. The boundary does not prevent privileged or malicious filesystem mutation, does not freeze the filesystem against external changes, does not make inventory identity an authenticated rollback anchor, and does not provide database commit/rollback semantics, distributed consensus, leases, or portable network-filesystem locking guarantees. The executable evidence covers cooperation on the Linux host-local filesystem contract used by CI: multiple shared readers coexist, an exclusive writer conflicts with live readers and other writers, readers conflict with a live writer, release is RAII-scoped, and durable publication advances the audited inventory only after the writer transaction can acquire the exclusive lock.",
    "This is an advisory serialization and optimistic-precondition claim only. Existing direct snapshot-store APIs remain outside the lock protocol for compatibility, and a caller that bypasses the transaction types is non-cooperating. A stale expected inventory is guaranteed to fail before the guarded archive reaches publication, but the boundary does not prevent privileged or malicious filesystem mutation, freeze the filesystem against non-cooperating changes, make inventory identity an authenticated/monotonic rollback anchor, or provide database rollback, multi-object atomic commit, distributed consensus, leases, or portable network-filesystem locking guarantees. The executable evidence covers cooperation on the Linux host-local filesystem contract used by CI: multiple shared readers coexist, an exclusive writer conflicts with live readers and other writers, a stale one-object token observes the actual two-object inventory without publishing the attempted third object, and a matching two-object token publishes durably then derives the exact three-object successor identity before releasing the writer lock.",
    "THREAT_MODEL 45A boundary",
)
