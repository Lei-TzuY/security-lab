name: materialize milestone 24B claims

on:
  push:
    branches: [tmp-24b-claims-generator]

permissions:
  contents: write

jobs:
  construct:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Materialize claims
        run: python3 .github/tmp-milestone-24b-claims.py
      - name: Enable user namespace capability tests
        run: |
          if [ -e /proc/sys/kernel/unprivileged_userns_clone ]; then
            sudo sysctl -w kernel.unprivileged_userns_clone=1
          fi
          if [ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]; then
            sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
          fi
      - name: Format check
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings
      - name: Full test suite
        run: cargo test --locked --all-targets --all-features
      - name: Verify conservative claims
        run: |
          grep -F 'current implementation deliberately cannot emit `satisfied` from the real probe path' README.md
          grep -F 'mandatory launch core is first-class evidence state' ROADMAP.md
          grep -F 'current production preflight can report known incompatibility or indeterminate evidence but cannot claim full satisfaction' THREAT_MODEL.md
      - name: Remove construction files and commit claims tree
        run: |
          rm .github/tmp-milestone-24b-claims.py
          rm .github/workflows/tmp-milestone-24b-claims.yml
          git config user.name LeiZ
          git config user.email 52287354+Lei-TzuY@users.noreply.github.com
          git add -A
          git commit -m "docs: synchronize milestone 24B preflight claims"
          git push origin HEAD:tmp-24b-claims-generator
